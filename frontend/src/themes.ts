import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#abb39c',
    },
    secondary: {
      main: '#f8f8f8',
    },
    background: {
      default: '#fcfaf2',
    },
  },
});

export default theme;
